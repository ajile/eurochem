var BaseRowsetModel = require('../../../common/models/rowset');

/**
 * Model of the abstract purpose, with unknown number of fields.
 * @class RowsetModel
 */
var RowsetModel = module.exports = BaseRowsetModel.extend({

      urlRoot: '/rowset/'

});
